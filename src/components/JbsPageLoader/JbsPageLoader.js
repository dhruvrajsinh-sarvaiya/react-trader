/**
 * Jbs Page Loader
 */
import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

const JbsPageLoader = () => (
    <div className="page-loader d-flex justify-content-center mb-30">
        <CircularProgress />
    </div>
);

export default JbsPageLoader;
